from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A computer vision model to detect the presence of malaria in image data',
    author='Matthew Nash',
    license='MIT',
)
